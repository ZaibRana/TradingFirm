# Progress

One row per part from `docs/plan-analyst-watcher.md`. Updated at the end of every part, before the commit.

| Part | Status | Commit | Date | Notes |
|---|---|---|---|---|
| 0.1 | done | 3ed39cf | 2026-09-04 | `.env.example` + real `.env` created; `docker-compose up -d` boots all 7 containers; `curl :8001/health` → `db_connected: true`. Blocker along the way: host `docker.sock` belonged to a different macOS user account and was unreachable until the user restarted Docker Desktop under this account. |
| 0.2 | done | 05128f5, 4ac7c34 | 2026-09-04 | Mounted `infra/supabase/migrations` read-only into postgres's `/docker-entrypoint-initdb.d/`; fresh `pgdata` volume now auto-applies `001_initial_schema.sql` (`\dn` shows `ai`, `data_engine`, `public`, `risk`, `signals`, `users`). `scripts/migrate.sh` handles existing volumes — tracks applied files in `public.schema_migrations`, rerun is a no-op. Went through two revisions after review: v1 ran `psql` on the host and `source`d `.env` for creds, which broke both on this Mac having no `psql` client and on `.env`'s unquoted `EDGAR_USER_AGENT` value containing a space (bash `source` treats it as two words). v2 runs entirely via `docker exec` into `tf-postgres`, reusing the container's own `psql` and `POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB` env vars — no host Postgres client or `.env` parsing at all. Also hit and fixed `mapfile` not existing on macOS's stock bash 3.2 (swapped for a `while read` loop). Verified by running `./scripts/migrate.sh` directly from the host shell (not inside a container). |
| 0.3 | done | ee0e131 | 2026-09-04 | Added `HEALTHCHECK` to all 4 FastAPI Dockerfiles (`python3 -c "urllib.request.urlopen(...)"` against `/health` — no curl/wget in `python:3.12-slim`, so reused the interpreter already in the image) and to `web/Dockerfile` (busybox `wget --spider` against `/`, since web has no `/health` route). `docker-compose.yml`: `web`'s `depends_on` on `data-engine` is now `condition: service_healthy` (the other three backend deps stay `condition: service_started`, unchanged behavior). Hit one bug: healthchecks that pointed at `localhost` passed for all 4 Python services but `tf-web` stayed `unhealthy` — inside the container `localhost` resolves to `::1` first and Next's standalone server only binds IPv4, so busybox `wget` got "connection refused" over IPv6 while a plain Node `http.get` (no getaddrinfo quirk) succeeded. Fixed by pointing every healthcheck at `127.0.0.1` instead of `localhost` for consistency. Verified: `docker compose up -d --build` then `docker compose ps` shows all 7 containers `(healthy)`, including `tf-web`. |
| 0.4 | done | 71b21f8 | 2026-09-05 | Removed the "Scanner 1" tab + all its state/handlers from `web/app/page.js`; deleted `web/app/api/scanner/{run,discover,filter,technical}` (the legacy exec path and the unused JS reimplementation's only callers), `web/lib/scanner/*`, `web/lib/data/*`, and `web/app/components/StockCard.js` (only consumer was Scanner 1). Removed `firebase` and `yahoo-finance2` from `package.json`; deleted `web/lib/firebase/*` and the Google-sign-in UI in `Header.js`/`AuthProvider` in `layout.js` since D18 parks auth. Added `scanner/README.md` marking the Python reference frozen. Updated `docs/overview.md` and `CLAUDE.md` scan-pipeline wording to describe the single live path. `npm install`/`npm run build` were blocked by the auto-mode Bash classifier in-session, so the user ran them directly — build succeeded, `package-lock.json` no longer lists the removed deps. Verified live: rebuilt `tf-web` (`docker compose up -d --build web`) since the prod container bakes code at build time and doesn't hot-reload; reloaded `localhost:3000` — Smart Scanner tab gone, `/api/scanner/run` now 404s, `/api/scanner/pro` still 200s and proxies to a healthy `data-engine`. Did not click "Run Scanner" itself to avoid triggering a real live Finviz/yfinance scan just to check wiring (G6). |
| 0.6 | done | e05c746 | 2026-09-05 | Added `docs/runbook.md`: Energy Saver settings (prevent sleep on power adapter, wake for network), `caffeinate -dims` as a belt-and-braces layer, Docker Desktop auto-start (start on login + restore last containers), and a numbered post-Wi-Fi-drop checklist (containers healthy → data-engine `/health` → in-flight scan status → watcher heartbeat once Phase 6 exists → notifier `/status` once Phase 5 exists). No test (plan marks this part n/a); no code changed. |
| 0.5 | done | 7bf96d1 | 2026-09-05 | Added `providers/fixture_provider.py` (`FixtureProvider(DataProvider)`, no network I/O — reads `tests/fixtures/{daily,hourly,info}/<TICKER>.json`); `tests/record_fixture_live.py`, a manual live-only recording script (filename avoids pytest's `test_*.py`/`*_test.py` discovery on purpose) that pulls real data via the existing `YFinanceProvider` (so it inherits its batching/canary/delay safety) and writes it with `df.to_json(orient="table")`, which round-trips the DatetimeIndex + dtypes losslessly; `tests/test_fixture_provider.py` (7 tests, synthetic temp fixtures, zero network). Chose JSON over parquet (plan allowed either) to avoid adding a `pyarrow` dependency the repo doesn't otherwise need. Hit two environment issues: (1) the host's global Python has pandas 3.0.3 vs the repo's pinned 2.3.0, which silently changed `read_json(orient="table")`'s datetime resolution (ns→us) — caught by running tests inside `tf-data-engine` (which has the pinned version) instead of trusting the mismatched host interpreter (G4); needed `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d data-engine` for a volume mount + ephemeral `pip install pytest pytest-asyncio` since neither is in `requirements.txt`, then `docker compose up -d data-engine` after to restore the prod build. (2) `assert_frame_equal` failed on `.index.freq` (`BusinessDay`/`Hour` vs `None`) — JSON round-tripping drops that convenience attribute, and real yfinance data doesn't carry it either, so fixed with `check_freq=False` rather than treating it as a real bug. Ran the recording script live per G6/G2: canary with `AAPL` alone first, verified real output (name/sector/marketCap/news, 252 daily + 448 hourly rows), then the full 3-ticker set (`AAPL MSFT SPY`) — one bulk daily call, one bulk hourly call, 3 `Ticker.info` calls at 2s delays, well inside batch/rate limits. Verified `FixtureProvider` end-to-end against the real recorded fixtures (correct row counts, last closes, MSFT sector/marketCap) before rerunning the full suite (`test_fixture_provider.py` + `test_scanner_pipeline.py`, 14/14 pass) as a regression check. |
| 0.5 follow-up | done | ad479ba | 2026-09-05 | From the Phase 0 review: (1) `providers/get_provider()` now accepts `fixture` → `FixtureProvider`, so `DATA_PROVIDER=fixture` works and Phase 1.2's endpoint test can select it by config instead of injecting; error message lists both names. `tests/test_provider_factory.py` (2 tests, no network — only the fixture branch is constructed). (2) `services/data-engine/requirements-dev.txt` (`-r requirements.txt` + pinned `pytest==9.1.1`, `pytest-asyncio==1.4.0`) so the per-session ephemeral `pip install` from Part 0.5 is now a documented one-liner; kept out of the prod image. `CLAUDE.md` Commands and `docs/overview.md` updated. Verified in the dev container: 16/16 pass across `test_provider_factory.py` + `test_fixture_provider.py` + `test_scanner_pipeline.py`, then prod build restored and `/health` green. |
| 0.4 follow-up | done | 1d1203e | 2026-09-05 | From the Phase 0 review: (1) `web/app/page.js` — `mapProResults` was a `const` inside `Home` referenced by the mount `useEffect` above its declaration (`react-hooks/immutability`, the one pre-existing `npm run lint` error; not a 0.4 regression, the same pattern predated it). It closes over no state, so hoisted to module scope. `npm run lint` now clean, `npm run build` green. No behaviour change, so `tf-web` was not rebuilt. (2) `docker-compose.yml` `LLM_PROVIDER` default `gemini` → `anthropic` to match D16 and `.env.example`. Verified with `docker compose --env-file <DB_PASSWORD-only file> config` rendering `LLM_PROVIDER: anthropic`. Runtime unchanged: the real `.env` already sets `anthropic`, and `ai-agent` is still a scaffold that reads nothing. |
| 1.1 | done | 717ecbf | 2026-09-05 | Migration `002_bars.sql`: `data_engine.ohlcv_bars` (ticker, interval, ts, open, high, low, close, volume; PK `(ticker, interval, ts)`, plus a `(ticker, interval, ts DESC)` index), `IF NOT EXISTS`-only per the re-runnable-migrations rule. `db.py`: `upsert_bars(pool, ticker, interval, bars)` (list-of-dict upsert, `ON CONFLICT` update, no-op on empty list) and `get_bars(pool, ticker, interval, since=None)` (ordered ascending, optional `ts >=` filter), mirroring the existing `upsert_stocks`/pool-acquire pattern. `tests/test_bars_store.py` (4 tests) mocks the asyncpg pool end-to-end (`MagicMock` pool → `AsyncMock` acquire context manager → `AsyncMock` connection) and asserts the literal SQL text and parameter tuples passed to `executemany`/`fetch` — no real DB, no network. Verified in `tf-data-engine`: new test file passes 4/4, then full regression (`test_fixture_provider.py` + `test_provider_factory.py` + `test_scanner_pipeline.py` + `test_bars_store.py`) 20/20. Also ran `./scripts/migrate.sh` against the live dev Postgres: applied `002_bars.sql` cleanly, `\d data_engine.ohlcv_bars` confirmed the schema, rerun was a no-op ("already applied"). Restored the prod `data-engine` build after; `/health` green with `db_connected: true`. |
| 1.2 | done | bf542e2 | 2026-09-05 | `POST /stock/{ticker}/refresh` in `main.py`: downloads daily (2y, `download_daily`) + hourly (3mo, `download_hourly`) via `app.state.provider` for one ticker, converts the extracted DataFrames to the lowercase dict shape `db.upsert_bars()` expects, upserts both intervals, `del`s the intermediate frames + `gc.collect()` (G8), returns `{"ticker", "dailyBars", "hourlyBars"}`. New `cache.py` helpers `get_refresh_cooldown`/`set_refresh_cooldown` (key `tf:cache:refresh:{ticker}`, TTL 900s) reject a repeat call inside 15 minutes with 429 + `Retry-After`; cooldown is written only *after* a successful upsert, so a failed fetch doesn't lock the ticker out. Ticker is normalized/validated the same way `/stocks/{ticker}` already does (`.upper().strip()`, alpha, ≤5 chars) so case can't be used to dodge the cooldown. `app.state.db_pool is None` returns 503 (not a fake 200 with 0 counts) — the endpoint's only job is persistence, so a DB outage must not look like success. Went through one review round after the first draft: the reviewer caught that a Redis-down fallback would fail *open* (no cooldown at all, a real risk per G6) with no test covering it — fixed by extending the existing `InMemoryStore` (the same object `/scan/run`'s cooldown already uses) with a per-ticker last-refresh dict as the fallback, and adding a dedicated test that runs with `redis=None` end-to-end. Endpoint logic is called directly in tests (`main.refresh_stock(...)`) rather than through `TestClient`/lifespan, avoiding any dependency on reachable Redis/Postgres hostnames during the test run — this repo's first endpoint-level test, so it introduces this pattern rather than testing framework wiring. The two 429 tests use a small hand-written in-process fake Redis (`get`/`set`/`ttl` only) instead of mocking the client away or adding a `fakeredis` dependency, so the real `cache.py` cooldown code path actually executes in the test. `tests/test_refresh_endpoint.py` (6 tests) + full regression (`test_fixture_provider.py`, `test_provider_factory.py`, `test_scanner_pipeline.py`, `test_bars_store.py`) — 26/26 pass in `tf-data-engine`. Prod `data-engine` rebuilt after; `/health` green (`db_connected`/`redis_connected` both true). Did not run a live single-ticker refresh against real yfinance — not required by the part's acceptance criteria and not worth the extra live call without asking first (G6). |
| 1.3 | done | 309a955 | 2026-09-05 | `MarketScanner` now takes a `db_pool` (wired from `app.state.db_pool` in `main.py`'s lifespan) and persists daily + hourly bars for final scan winners once hourly filtering finishes, via the same `db.upsert_bars()` used by 1.2's refresh endpoint. Per-ticker `daily_df`/`hourly_df` are captured only at the moment a ticker survives that stage's filter (added to `daily_winners`/`winners` in the same branch) — non-survivors are never captured, so peak held frames is bounded by that stage's own funnel width, not the ~650-candidate pool; both frame dicts are `del`'d + `gc.collect()`'d alongside the pipeline's existing bulk-download cleanup (G8). Persistence is best-effort: `db_pool is None` or an `upsert_bars` exception for one ticker is logged and skipped, never aborts the scan. Found and fixed a real pre-existing bug during the G1.5 spec pass (not hypothetical): `get_candidates()` (Finviz-sourced) never normalized ticker case/whitespace, so scanner-derived keys weren't guaranteed to match the `.upper().strip()` form `main.py`'s endpoints already used for the same `ohlcv_bars` rows — confirmed via `Explore` agent, not assumed. Fixed by extracting `main.normalize_ticker()` as the one shared normalization function (G1.5), applied in the scanner at the exact point a ticker enters `daily_winners`; verified with `grep -n "\.upper()" main.py` returning only the definition. Also moved `_bar_records_from_df` out of `main.py` into `db.py` as `bar_records_from_df()` so both write paths share one bar-shaping implementation instead of duplicating it (main.py's `/stock/{ticker}/refresh` updated to import it). Went through two spec revisions before coding (G1): reviewer flagged that the capture strategy fought G8 without naming a pruning point, that the writes/failure-branch tables had drifted from the `write \| before/after success \| state-if-fails-after` and `branch \| behavior \| fails open/closed \| test` schemas (silently dropping the column that catches partial-write bugs), and that "no new normalization needed" was an unverified assumption — all three closed before implementation. Per the writes table: `db.upsert_bars()`'s `executemany` has no explicit `conn.transaction()` wrapper, so a raise partway through can leave one ticker's bars partially upserted for that call — pre-existing in `db.py`, logged as deferred work, not fixed in this part. `tests/test_scanner_pipeline.py` — 5 new tests (`TestPipelinePersistsWinnerBars`): persists exactly `{("GOOD","1d"),("GOOD","1h")}` with normalized keys and nothing for a non-daily-winner or a daily-winner-that-fails-hourly; skips persistence entirely when `db_pool` is `None`; continues (both interval attempts still made) when `upsert_bars` raises; makes zero calls when `daily_winners` is non-empty but `winners` ends empty; and confirms repeat persist calls re-upsert every time with no client-side dedup (idempotency is `db.py`'s `ON CONFLICT DO UPDATE`, not application logic). Verified in `tf-data-engine`: 12/12 in `test_scanner_pipeline.py`, then full regression (`test_fixture_provider.py`, `test_provider_factory.py`, `test_scanner_pipeline.py`, `test_bars_store.py`, `test_refresh_endpoint.py`) 31/31 — confirms the `bar_records_from_df`/`normalize_ticker` moves didn't break 1.2's endpoint. Prod `data-engine` rebuilt after; `/health` green (`db_connected`/`redis_connected` both true). |
