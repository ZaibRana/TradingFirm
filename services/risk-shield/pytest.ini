# Discovery is restricted the same way data-engine's is (Part 0.7): only
# tests/test_*.py is collected, so a bare `pytest` can never pick up a
# live-API script added later under tests/.
[pytest]
testpaths = tests
python_files = test_*.py
